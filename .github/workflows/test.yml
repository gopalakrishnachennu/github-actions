name: Test Composite Python Action

on:
  push:
    branches: [ main ]

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout this repo
        uses: actions/checkout@v4

      - name: Run our custom composite Python Action
        uses: ./
        with:
          name: "Gopall"
